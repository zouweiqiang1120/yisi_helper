# 2026-04-03 工作记录

## GitHub Actions 构建修复

**用户**: songjiayang / 周伟强 (Gitee)
**项目**: yisi_helper (Flutter Android 应用)

### 问题
GitHub Actions 构建 APK 失败，错误信息：
- `This app is using a deprecated version of the Android embedding`
- `The plugin "permission_handler_android" requires your app to be migrated to the Android embedding v2`
- 权限问题：`cannot write to file` (local.properties)

### 修复内容

#### 1. 修复 `android/build.gradle`
移除旧版 `buildscript` 块，改用新版 Flutter 插件管理方式：

```gradle
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
```

#### 2. 修复 `.github/workflows/build.yml`
- 添加权限修复步骤 (`sudo chown`, `sudo chmod`)
- 添加 `local.properties` 自动创建
- 添加 `flutter clean` 清理缓存

#### 3. 创建本地修复脚本
- `fix_build.sh` - Linux/Mac 用
- `fix_build.bat` - Windows 用

### 后续方案
用户因网络问题无法推送 GitHub，转用 **Gitee（码云）**：
- 仓库地址: https://gitee.com/周伟强/yisi_helper
- 计划使用 Gitee CI (`.gitee-ci.yml`) 替代 GitHub Actions

### 待完成
- [ ] 在 Gitee 上修改 `android/build.gradle`
- [ ] 创建 `.gitee-ci.yml` 构建配置
- [ ] 运行 Gitee 流水线构建 APK

## 技能/工具使用
- GitHub Actions 工作流配置
- Flutter Android 构建
- Gradle 配置修复
- Git 操作（本地提交、推送）
- Gitee 平台介绍
