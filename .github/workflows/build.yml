name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
    
    - name: Check and prepare project
      run: |
        ls -la
        if [ -d "yisi_helper" ]; then
          echo "Project already extracted"
        elif [ -f "yisi_helper.zip" ]; then
          echo "Extracting zip file..."
          unzip -o yisi_helper.zip
        fi
        ls -la yisi_helper/
    
    - name: Install dependencies
      working-directory: ./yisi_helper
      run: |
        ls -la
        flutter pub get
    
    - name: Build APK
      working-directory: ./yisi_helper
      run: |
        ls -la
        flutter build apk --release --verbose
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: yisi-helper-apk
        path: yisi_helper/build/app/outputs/flutter-apk/app-release.apk
